# Recon & Enumeration Toolkit v2.0 — Python dependencies
# Install with: pip install -r requirements.txt

# Required
dnspython>=2.3.0
requests>=2.28.0

# Optional but strongly recommended
python-whois>=0.8.0
rich>=13.0.0
pyyaml>=6.0          # needed for --config support
anthropic>=0.25.0    # needed for --ai features (pip install anthropic)
